# 公共区域功能设计文档

***

## 1. 背景与目标

| 项目      | 说明                                                                                                                                                |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| 背景      | 平台需要一个“公共区域”承载团队共享文件，文件落盘到宿主机持久化目录，容器销毁后仍保留                                                                                                       |
| 目标（MVP） | 1) 启动容器时自动挂载公共区域全部目录与文件；2) 普通用户在自己 Docker 中对公共区域的上传/修改/删除仅作用于容器内副本，不影响宿主机；3) 管理员在 Docker 中对公共区域的操作可影响宿主机；4) 去掉在线编辑与在线查看；5) 显示服务器完整路径并阻断越级回退；6) 分页 |
| 版本      | v0.3                                                                                                                                              |

***

## 2. 术语与范围

| 术语           | 含义                                                                                |
| ------------ | --------------------------------------------------------------------------------- |
| 宿主机公共区域根目录   | `/var/lib/clawloops/shared/public/files/`，平台公共文件的权威数据源                            |
| 用户容器公共区域副本   | 用户 Runtime 启动时，从宿主机公共区域复制得到的容器内私有副本；用户在容器内操作只影响该副本                                |
| 管理员容器公共区域工作区 | 管理员 Runtime 启动时，直接挂载宿主机公共区域；管理员在容器内操作会影响宿主机                                       |
| 目录路径         | 通过 URL Query 传递的相对路径 `path`（例如 `docs/spec`），用于定位公共区域内子目录                          |
| 完整路径显示       | 前端路径栏显示服务器完整绝对路径，例如 `/var/lib/clawloops/shared/public/files/test`（`test` 为用户新建目录） |

***

## 3. 权限矩阵

| 场景/能力         | user | admin |
| ------------- | ---- | ----- |
| 宿主机公共区目录列表    | ✅    | ✅     |
| 宿主机公共区上传（新增）  | ✅    | ✅     |
| 宿主机公共区下载      | ✅    | ✅     |
| 容器内副本上传/修改/删除 | ✅    | ✅     |
| 宿主机公共区删除      | ❌    | ✅     |
| 宿主机公共区覆盖上传    | ❌    | ✅     |
| 容器内操作影响宿主机    | ❌    | ✅     |

***

## 4. 架构与职责分工

| 组件              | 职责                                                                |
| --------------- | ----------------------------------------------------------------- |
| clawloops-web   | 公共区域页面（目录浏览、创建目录、上传、下载、删除、分页）；显示服务器完整路径；阻断回退到公共根目录以上并弹窗提示“不允许再回退” |
| clawloops-api   | 会话校验、角色判断、对外 API 语义收口；按角色控制宿主机影响权限                                |
| runtime-manager | 容器启动阶段执行“用户复制副本 / 管理员直挂宿主机”策略；公共文件目录创建、列表分页、路径安全校验                |
| 宿主机目录           | 公共文件权威数据源：`/var/lib/clawloops/shared/public/files/`               |

***

## 5. 数据流

### 5.1 公共文件数据流

| 步骤 | 说明                      |
| -- | ----------------------- |
| 1  | 用户或管理员上传文件到公共区域（宿主机）    |
| 2  | 服务端写入宿主机公共区域目录          |
| 3  | 列表 API 按目录分页返回（每页 10 条） |
| 4  | 下载时直接从宿主机公共区域读取并返回      |

### 5.2 容器挂载与隔离数据流

| 步骤 | user Runtime                    | admin Runtime               |
| -- | ------------------------------- | --------------------------- |
| 1  | 启动容器                            | 启动容器                        |
| 2  | 将宿主机公共区域完整复制到用户容器公共区域副本         | 将宿主机公共区域直接挂载到管理员容器工作区       |
| 3  | 用户在容器内上传/修改/删除仅作用于容器副本，不回写宿主机   | 管理员在容器内上传/修改/删除会直接影响宿主机公共区域 |
| 4  | 用户容器销毁后副本随容器生命周期结束（宿主机公共区域不受影响） | 管理员容器销毁不影响宿主机公共区域中已产生的变更    |

***

## 6. 存储设计（落盘与挂载）

### 6.1 宿主机目录规划

| 类别        | 宿主机目录（源数据）                                | 说明               |
| --------- | ----------------------------------------- | ---------------- |
| 公共文件权威目录  | `/var/lib/clawloops/shared/public/files/` | 存放任意类型公共文件与目录结构  |
| 用户容器副本源   | 启动时由权威目录复制                                | 用户容器内读写，不回写宿主机   |
| 管理员容器工作目录 | 启动时挂载权威目录                                 | 管理员容器内操作直接作用于宿主机 |

### 6.2 角色差异化挂载策略

| 角色    | 容器启动策略            | 对宿主机影响 |
| ----- | ----------------- | ------ |
| user  | 自动复制公共区域到容器私有副本   | 否      |
| admin | 自动挂载宿主机公共区域到容器工作区 | 是      |

***

## 7. API 设计（对外）

统一由 `clawloops-api` 暴露，路径前缀：`/api/v1/public-area/*`。

### 7.1 公共文件 API

| 方法     | 路径                            | 入参                                       | user     | admin  | 返回/说明                                            |
| ------ | ----------------------------- | ---------------------------------------- | -------- | ------ | ------------------------------------------------ |
| GET    | `/public-area/files/list`     | `path` 可选（目录）；`page` 可选（默认 1）            | ✅        | ✅      | `{ entries, page, pageSize, total, totalPages }` |
| POST   | `/public-area/files/mkdir`    | `path` 必填（相对路径，指向要创建的目录）                 | ✅        | ✅      | `{ success: true }`                              |
| POST   | `/public-area/files/upload`   | `path` 必填（相对路径）；`file` 必填；`overwrite` 可选 | ✅（强制不覆盖） | ✅（可覆盖） | 上传文件；父目录自动创建                                     |
| GET    | `/public-area/files/download` | `path=<path>`                            | ✅        | ✅      | 下载 bytes                                         |
| DELETE | `/public-area/files/delete`   | `path=<path>`                            | ❌        | ✅      | 删除文件/目录；目录仅允许空目录删除                               |

***

## 8. 校验规则与错误语义

### 8.1 公共文件路径安全

| 校验项  | 规则                                                          |
| ---- | ----------------------------------------------------------- |
| 允许范围 | 仅允许公共区域根目录下的相对路径（服务端会将 `\` 统一替换为 `/`；若输入以 `/` 开头会被规范化为相对路径） |
| 禁止项  | `.`/`..` 段、路径穿越、越过公共区域根目录                                   |

### 8.2 冲突与权限

| 场景                       | 行为 | 期望错误          |
| ------------------------ | -- | ------------- |
| 同名存在且 overwrite=false    | 拒绝 | 409           |
| user 试图 overwrite/delete | 拒绝 | 403           |
| 删除非空目录                   | 拒绝 | 409           |
| 回退到公共区域根目录上一级            | 阻断 | 前端弹窗：“不允许再回退” |

***

## 9. 前端页面设计（控制台）

| 页面       | 路由                     | 角色    | 模块                                                              | 权限差异                    |
| -------- | ---------------------- | ----- | --------------------------------------------------------------- | ----------------------- |
| 公共区域     | `/public-area`         | user  | 目录浏览（固定根目录不可上溯）、完整路径显示、新建目录、上传（不覆盖）、下载、分页（10/页）                 | user 无覆盖上传、无删除宿主机权限     |
| 公共区域管理   | `/admin/public-area`   | admin | 目录浏览（固定根目录不可上溯）、完整路径显示、新建目录、上传（可覆盖）、下载、删除、分页（10/页）              | admin 可覆盖上传、可删除宿主机文件/目录 |
| URL 状态约束 | `?path=<dir>&page=<n>` | -     | `path` 表示当前目录相对路径并映射到完整绝对路径展示；`page` 表示当前页（每页固定 10 条）；支持上一页/下一页 | -                       |

### 9.1 列表排序与显示

| 项目   | 规则                                                        |
| ---- | --------------------------------------------------------- |
| 排序   | 目录优先，其次按名称（不区分大小写）升序                                      |
| 图标展示 | 目录显示文件夹图标；文件按扩展名映射不同图标以示区别（前端呈现）                          |
| 路径展示 | 显示服务器完整路径，如 `/var/lib/clawloops/shared/public/files/test` |
| 回退阻断 | 点击返回上级时若将越过公共区域根目录，则阻断并弹窗“不允许再回退”                         |
| 目录嵌套 | 支持无限嵌套创建目录，进入后路径持续追加展示                                    |

***

## 10. 运维与治理建议

| 主题 | 建议                             |
| -- | ------------------------------ |
| 配额 | 限制总容量、单文件大小、上传频率，避免磁盘被打满       |
| 审计 | 记录上传/覆盖/删除（用户、时间、IP、对象路径、hash） |
| 备份 | 共享目录纳入备份（public files）         |
| 回收 | 建议后续引入软删除/回收站避免误删不可逆           |
| 隔离 | 用户容器副本与宿主机公共区隔离，避免用户误操作污染权威数据  |

***

## 11. 验收标准（MVP）

| 项目    | 验收点                                                                                       |
| ----- | ----------------------------------------------------------------------------------------- |
| user  | 启动用户容器后自动复制公共区域全部文件与目录；用户在容器内上传/修改/删除不会影响宿主机公共区域；路径显示完整绝对路径；越级回退被阻断并提示“不允许再回退”；分页 10/页可翻页 |
| admin | 启动管理员容器后自动挂载宿主机公共区域；管理员在容器内上传/修改/删除会影响宿主机；支持覆盖上传与删除空目录；路径与分页行为同 user；无在线编辑与在线查看能力         |

