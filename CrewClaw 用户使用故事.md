1. 用户第一次接触系统
1.1 用户访问平台首页

用户打开 CrewClaw 平台域名，例如：

https://crewclaw.example.com

这时他看到的不是一堆配置项，而是一个很明确的入口：登录进入平台。

在底层，Traefik 负责把流量转给控制面，Authentik 负责统一登录。但对用户来说，这一切都隐藏掉了，用户只感知到：

这是公司统一平台

需要登录

登录后就能进入自己的工作台

1.2 用户登录

用户通过 Authentik 完成认证。

首次登录时，系统自动做几件事，但用户几乎无感：

模块 1 校验身份

模块 1 调用 /internal/users/sync

模块 2 发现这是首次登录用户，于是自动创建 CrewClaw 平台用户记录

用户被赋予默认 tenantId=t_default

默认角色是 user

默认状态是 active

用户不需要申请 workspace，也不需要手工建账号，不需要点“初始化环境”。

1.3 登录后的第一印象

用户进入平台后，看到的是一个极简工作台，通常会有：

当前登录身份

runtime 当前状态

“启动 Runtime”按钮

“进入工作区”入口

只读模型信息

最小 usage summary

不会有这些复杂项：

API Key 管理

模型绑定下拉框

provider 选择器

gateway 配置表单

环境变量面板

这正是新设计的目的：让用户一进来就用，而不是一进来先配。

2. 用户第一次启动自己的 runtime

这是用户真正“开始使用系统”的关键动作。

2.1 用户点击“启动 Runtime”

用户在工作台上点击一个很直接的按钮：

启动 Runtime

对用户来说，他的理解应该很简单：

“给我准备好我的个人工作区，我要开始用了。”

2.2 系统在背后自动完成首次初始化

如果这是用户第一次启动，后台会自动补齐所有运行时资源。

第一步：确保 binding 存在

模块 3 不会自己乱生成 runtime 信息，而是先找模块 2：

这个用户有没有 UserRuntimeBinding？

如果没有，就由模块 2 创建

模块 2 会一次性分配好这些资源：

runtimeId

volumeId

默认 imageRef

默认 retentionPolicy=preserve_workspace

也就是说，用户第一次点击启动，等价于“平台自动为他开通个人运行空间”。

用户看不到这些字段，但正是这些字段保证了后续：

他的 workspace 是持久的

他的 runtime 是可恢复的

他的数据保留策略是清晰的

2.3 系统自动准备运行配置

接下来模块 3 会自动做用户本来不该操心的事：

从模块 4 获取这个用户可用的平台统一模型配置

渲染 runtime 所需的 config file

生成 secret file

组装启动模板

这个模板里包括：

用哪个镜像启动

挂哪个 volume

分配哪个 routeHost

挂哪些配置文件

用哪种 retentionPolicy

用户不需要看到“模型网关地址是什么”“token 放哪儿了”“容器怎么挂载的”。

2.4 runtime manager 拉起容器

然后 runtime manager 真正执行运行时编排：

创建或启动容器

挂载持久化卷

挂载只读配置文件

挂载只读 secret file

返回 observedState

返回 internalEndpoint

2.5 平台生成用户可访问的 browserUrl

容器起来后，Traefik 激活该用户的子域名路由，比如：

https://u-001.crewclaw.example.com

但这个地址不是匿名公开地址，它会被 Authentik 前置鉴权保护。

因此从用户视角看，他拿到的是：

一个自己的工作区入口

一个可以在浏览器打开的地址

一个已经被登录系统保护的地址

而不是一个裸露的容器端口。

3. 用户等待 runtime 就绪
3.1 用户看到“启动中”

用户点击“启动 Runtime”后，不一定立刻能进入工作区。前端通常会轮询两个东西：

任务状态

runtime 轻量状态

用户在界面上看到的通常是：

启动中

正在准备工作区

稍后可进入

这时系统在内部经历的可能状态包括：

pending

running

runtime creating

3.2 用户不需要知道底层细节

用户不需要知道：

Docker image 正在 pull

容器网络是否 ready

routeHost 是否已经下发

config 文件是否已经挂载

对用户最好的体验是只显示业务意义：

未启动

启动中

可进入

启动失败

3.3 如果启动成功

当前端通过 /api/v1/workspace-entry 拿到：

ready=true

browserUrl=...

那用户就会看到“进入工作区”按钮可用。

3.4 如果启动失败

如果出现异常，用户会看到的是一种可理解但不暴露底层实现的提示，例如：

工作区启动失败，请稍后重试

当前运行环境不可用

请联系管理员

而不是：

container create failed

docker network attach error

mount secret file failed

lastError 可以存在系统里，也可以被前台适度展示，但展示口径应偏产品化，而不是暴露内部基础设施细节。

4. 用户进入自己的 workspace
4.1 用户点击“进入工作区”

当 ready=true 后，用户点击“进入工作区”。

前端跳转到 browserUrl。

4.2 为什么用户能直接用，而不需要配置模型

因为在这个新设计里，用户进入 workspace 时，系统已经提前准备好了：

模型网关地址

可用模型列表

默认模型路由

网关访问凭据

runtime 需要的配置文件

所以用户进入 OpenClaw 后，理想体验应该是：

打开就能开始用 AI 能力，而不是先被要求填一堆模型配置。

4.3 首次进入时的本地 profile 自动建立

如果这是第一次真正进入 runtime，平台会自动完成本地 profile 的创建和映射。

对用户来说，这意味着：

这是“我的工作区”

我再次回来还是这个工作区

我的本地状态会跟着 volume 保留

用户不需要自己点“创建 profile”。

5. 用户在 workspace 内的真实使用体验

这是最重要的一段，因为它定义了“这个产品到底让用户得到什么”。

5.1 用户把它当作一个现成可用的 AI 工作空间

用户进入自己的 OpenClaw runtime 后，主要心智应该是：

这是我个人的 AI 工作区

这是公司已经配好的环境

我不用管模型供应商，只要用

我不用管 token，只要用

我不用管容器，只要用

5.2 用户能看到平台开放的模型能力

用户可以通过平台提供的模型服务执行任务，但用户看到的应是“可用模型”或“平台支持的能力”，而不是复杂的供应商绑定拓扑。

例如用户能感知的是：

有默认模型可用

平台可能开放几个模型选项

某些模型可见，某些不可见

但用户不会去：

绑定自己的 OpenAI key

配 Anthropic key

切 Azure endpoint

配 baseUrl

写代理地址

5.3 用户的目标是完成工作，不是维护配置

用户在 workspace 中的真实行为通常是：

开始和 agent 交互

使用平台模型完成任务

维护自己的工作内容

继续下次未完成的工作

在这个设计里，用户的注意力被保护住了。
系统替他屏蔽了平台层噪音。

6. 用户暂时离开系统
6.1 用户关闭浏览器或离开页面

用户离开时，runtime 不一定立刻停止。

因为 runtime 的生命周期是平台控制的，不是浏览器 tab 控制的。

这意味着：

用户刷新页面不会丢工作区

重新登录后仍可回到已有 runtime

已存在的 workspace 可以被恢复

6.2 用户再次回来

当用户下次再回来：

登录平台

查看 runtime 状态

如果 runtime 仍在运行，直接进入工作区

如果 runtime 已停止，可以重新启动

如果 volume 保留，工作区状态会继续存在

对用户来说，这是一种“个人工作台持续存在”的体验，而不是一次性会话。

7. 用户主动停止 runtime
7.1 为什么用户会停止 runtime

用户可能会出于这些原因点击“停止 Runtime”：

暂时不用了

想释放资源

工作结束了

避免空闲运行

7.2 用户点击“停止 Runtime”后发生什么

用户点击停止后，平台做的是：

给 runtime 下发目标状态 stopped

runtime manager 停止容器

保留 volume

保留 workspace/state/profile 数据

这点非常重要。

在默认策略 preserve_workspace 下，停止不等于删除。
所以用户心智应是：

我只是把工作台关掉了，不是把工作清空了。

7.3 用户下次还能恢复

以后用户再点启动：

binding 还在

volume 还在

imageRef 还在

retentionPolicy 还在

因此 runtime 可以被重新拉起，工作区恢复。

8. 用户删除 runtime

这是和“停止”明显不同的行为。

8.1 用户为什么会删除 runtime

用户可能会因为：

想重置环境

不再需要工作区

管理员要求清理

想清空数据重新开始

8.2 删除时平台必须把语义说清楚

系统必须清楚区分两种删除语义：

第一种：保留工作区删除

preserve_workspace

效果是：

删除容器

删除路由

保留 volume

保留 profile 数据

用户下次再启动时，工作内容可以恢复。

第二种：彻底清空删除

wipe_workspace

效果是：

删除容器

删除路由

删除 volume

清空 workspace 和 profile 数据

用户下次再启动时，相当于全新环境。

8.3 MVP 默认应偏保守

MVP 默认是 preserve_workspace。
这符合“用户只使用”的产品原则，因为普通用户通常不应该因为一个误操作就丢掉所有工作数据。

9. 用户查看自己的最小状态信息

虽然用户不配置系统，但不代表用户什么都看不到。

9.1 用户能看到 runtime 状态

用户工作台上至少应该能看到：

未启动

启动中

运行中

已停止

异常

这让用户知道当前是否可进入工作区。

9.2 用户能看到自己的工作区入口是否 ready

用户不用理解 observedState 和 desiredState 的全部含义，但界面层面需要能表达：

还不能进入

正在准备

可以进入

出错了

9.3 用户能看到最小 usage summary

在新设计下，用户不管理计费，也不做平台治理，但可以保留最小必要可见性，比如：

最近使用量摘要

token 使用趋势简表

本人工作区的最小 usage 信息

但不必把它做成复杂财务中心。

10. 用户被禁用时的体验

这是一个重要的治理场景。

10.1 管理员禁用用户

当管理员把用户状态改成 disabled 后，系统立即收口：

该用户 runtime 的 desiredState 被收敛到 stopped

runtime manager 停止容器

已签发 gateway token 立即失效

10.2 用户侧看到什么

此时用户再访问平台时：

/api/v1/auth/me 仍可识别身份

但其他业务接口统一返回 403 USER_DISABLED

从产品体验上，用户应感知为：

账号身份还在

但业务访问被停用

无法再进入工作区

无法再发起 runtime 操作

这比返回各种不一致的“not ready”更清晰。

11. 这个完整 story 里的核心产品价值

如果把整套 story 浓缩成一句话，就是：

用户面对的是一个“已经由管理员准备好的个人 AI 工作区”，用户只负责进入并使用，不负责配置底层能力。

这个设计带来的价值很直接。

对用户

学习成本低

首次上手快

不容易配错

不会暴露密钥和底层基础设施

更像企业产品，而不是开发者拼装台

对管理员

权限边界清晰

模型和凭据统一治理

更容易控风险

更容易做标准化支持

更容易做后续运维和审计

对系统实现

用户侧接口更简洁

运行时启动链路更稳定

模块职责更明确

安全边界更清楚

产品叙事更统一


v 0.3
reno 
2026-03-13 17:59